// Create a C program to determine if a given number is even or odd using only ternary operators.
// Take the number as input from the user and output whether it's even or odd.

/*
#include<stdio.h>
int main ()
{
    int b,x,even,odd;
    scanf("%d",&b);

    b%2==0?printf("even"):printf("odd");
    
    return 0;
}
*/

// Write a C program that calculates and displays the area of a rectangle.
// Take the length and width as input from the user.

/*
#include<stdio.h>
int main()
{
    float length,width,area;
    scanf("%f%f",&length,&width);

    area=length*width;

    printf("%f",area);

    return 0;
}
*/

/*#include<stdio.h>
int main()
{
    int a=0x1;
    printf("%o",a);
    return 0;
}*/

/*
#include<stdio.h>
#include<math.h>
int main()
{
    double s;
    scanf("%lf",&s);
    double sq = sqrt(s);
    printf("%lf",sq);

    return 0;
}*/